import React from "react";

const PopularList = () => {
	// const { data, isLoading, isError, error } = ();

	// if (isLoading) {
	// 	return <h1>Loading...</h1>;
	// }
	// if (isError) {
	// 	return <div variant="danger">{error.message}</div>;
	// }
	return (
		<div id="popular-list">
			<div>
				<h2>Best Cocktail</h2>
				{/* <Card title="Upcoming Movies" movies={data.results} /> */}
			</div>
		</div>
	);
};

export default PopularList;
